ext {
  project_group = "me.dmexe.telemetry"
  project_version = project.hasProperty('project.version') ?
      project.getProperty('project.version') :
      new File("${rootProject.rootDir}/VERSION").text.trim()

  netty_version = "4.1.16.Final"
  opentracing_version = "0.30.0"
  prometheus_version = "0.0.26"
  findbugs_version = "3.0.2"
  slf4j_version = "1.7.25"
  logback_version = "1.2.3"
  okhttp_version = "3.8.1"
  jetbrains_annotations_version = "15.0"
  kafka_version = "0.10.2.1"
}